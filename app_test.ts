// Transavner is a browser-only app (React + Web APIs).
// This file satisfies the `deno test` requirement in deno.yml.
Deno.test("app is a browser app with no server-side logic", () => {
  // No Deno-side code to test — all logic runs in the browser.
});
